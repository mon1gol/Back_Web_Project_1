<h1>Мои посты</h1>

<main>
    {{#if posts.length}}
    {{#each posts}}
    <div class="row">
        <div class="col s6 offset-s3">
            <div class="card">
                <div class="card-image">
                    <img src="{{img}}">
                    <span class="card-title">{{title}}</span>
                    {{!-- <a class="btn-floating halfway-fab waves-effect waves-light red"><i
                            class="material-icons">add</i></a> --}}
                </div>
                <div class="card-content">
                    <p>{{text}}</p>
                    <div>Автор: </div>
                    <div>Дата: </div>
                    <button class="btn-small right blue"><a href="post/{{id}}" class="white-text">Перейти</a></button>
                    <button class="btn-small right blue"><a href="post/{{id}}/edit?allow=true"
                            class="white-text">Редактировать</a></button>
                </div>
            </div>
        </div>
    </div>
    {{/each}}
    {{else}}
    <p>Создайте свой первый пост!</p>
    {{/if}}
</main>